# Agent-Harness-Only Template

이 template repository는 agent harness만 담고 application framework, product code, runtime-specific starter app은 포함하지 않는다. 목적은 Codex와 Claude Code를 포함한 agent 협업 방식을 표준화하는 것이며, 특정 stack starter가 필요하면 별도 template variant로 분리한다.
