# Ledger Loop

Ledger Loop is a fresh product repository for reconciling small-business
invoices. The initial technical choices are already decided: TypeScript on
Node.js, npm as the package manager, and SQLite for local development.

Use these project commands when recording the operating contract:

- install: npm install
- test: npm test
- build: npm run build
